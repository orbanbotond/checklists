<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<% #TODO We need to specify the subaccount Here  %>

<p><%= t("devise.mailer.invitation_instructions.someone_invited_you", url: "account_tbd/#{}/") %></p>

<p><%= link_to t("devise.mailer.invitation_instructions.accept"), accept_user_invitation_url(@resource, :invitation_token => @token) %></p>

<p><%= t("devise.mailer.invitation_instructions.ignore").html_safe %></p>
